# Scripts

Start and stop scripts for the Docker container.

- `start.sh` / `stop.sh` -- Linux and Mac (bash)
- `start.ps1` / `stop.ps1` -- Windows (PowerShell)

Start builds and runs the container. Stop tears it down.
